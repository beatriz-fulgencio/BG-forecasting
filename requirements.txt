# Runtime dependencies. Every package here is imported by the benchmark; see
# setup.py, which installs this list as install_requires.

# Core numerics and data handling
numpy>=1.21.0
pandas>=2.2.0
scipy>=1.9.0

# Modelling and metrics
torch>=1.9.0
scikit-learn>=1.0.0

# Statistics: Student-t seed intervals, Wilcoxon families, Holm correction
statsmodels>=0.13.0

# Figures
matplotlib>=3.5.0
# Only benchmark/analysis/experiments/patient_analysis.py imports this, and only
# inside the UMAP branch of the cluster figures; the import is deferred so the
# rest of the analysis stack does not pay for it.
umap-learn>=0.5.0

# Configuration
pyyaml>=6.0

# Development extras are declared in setup.py under extras_require["dev"]:
#   pip install -e ".[dev]"
